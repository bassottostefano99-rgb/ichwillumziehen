{
  "name": "ichwillumziehen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mietrechner + Umzugsbudget — ichwillumziehen.com",
  "scripts": {
    "test": "bash -c 'for f in tests/*.test.js; do node --test \"$f\"; done'",
    "test:watch": "node --test --watch tests/",
    "serve": "python -m http.server 5500",
    "og:render": "node scripts/render-og.js"
  },
  "devDependencies": {
    "puppeteer": "^23.0.0"
  }
}
